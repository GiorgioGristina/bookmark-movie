
<div class="banner-show" style='background-image: url("<%= cl_image_path @list.photo.key%>")'>
       
      <h2><%= @list.name %></h2>  
</div>

<%= link_to 'back to lists', root_path %>
<%= link_to 'edit', edit_list_path %>
<%= link_to 'delete', list_path(@list),
                      method: :delete,
                      data: {confirm: 'are you sure?'}%>

<hr>
<%= link_to 'add movie', new_list_bookmark_path(@list) %>

<div class="bookmark-container">
    <% @list.movies.each do |movie| %>
        <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
        
        <div class="movie_card">
        <%= image_tag "https://image.tmdb.org/t/p/w500#{movie.poster_url}", alt: movie.title, class: "movie-img rounded-top" %>
        <div class="movie-text">
            <h2><%= movie.title %></h2> 
            <p class='rating'><%= movie.rating %>⭐</p>
            <p><%= movie.overview %></p>
            <%= link_to 'delete', bookmark_path(bookmark), method: :delete, data: {confirm: "are you sure you want to remove#{movie}"}%>
        </div> 
        </div>

    <% end %>
</div>


<%= link_to 'delete', bookmark_path(bookmark), method: :delete, data: {confirm: "are you sure you want to remove#{movie}"}


